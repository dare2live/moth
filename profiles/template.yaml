kind: template
name: example
repo_path: /absolute/path/to/repo
codegraph_root: .
complexity_command:
  - python
  - ~/.codex/skills/complexity-optimizer/scripts/analyze_complexity.py
  - /absolute/path/to/repo
  - --format
  - markdown
evidence_paths:
  goal: goal.md
  handoff: SESSION_HANDOFF.md
  workflow_checkpoint: analysis/workflow_checkpoint.md
  quickstart: docs/chunkyctl_session_quickstart.md
  docs: docs
notes: Replace with repo-specific paths and checks.
